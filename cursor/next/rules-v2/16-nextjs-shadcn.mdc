---
description: Guidelines for setting up and using shadcn/ui components in Next.js applications. Apply when integrating shadcn/ui, managing component structure, and extending the component library.
globs:
alwaysApply: false
---

# Next.js shadcn/ui Guidelines

## 1. Installation and Setup (P0)

1. CRITICAL: Initial setup with Next.js:
   ```bash
   # Initialize shadcn/ui (interactive mode)
   pnpx shadcn-ui@latest init
   
   # Non-interactive mode with default settings
   pnpx shadcn-ui@latest init -d --yes
   
   # With custom settings
   pnpx shadcn-ui@latest init --yes --typescript --style=default --base-color=slate --css-file=app/globals.css --rsc --src-dir
   ```

2. REQUIRED: Key configuration options:
   ```
   --typescript    # Use TypeScript for component types
   --style         # Style to use (default, new-york)
   --base-color    # Base color for theming (slate, zinc, etc.)
   --rsc           # Support React Server Components
   --src-dir       # Whether to use src directory
   ```

3. ESSENTIAL: Adding components:
   ```bash
   # Add a single component
   pnpx shadcn-ui@latest add button
   
   # Add multiple components
   pnpx shadcn-ui@latest add --yes button card dialog form input
   
   # Add all available components
   pnpx shadcn-ui@latest add --yes --all
   ```

## 2. Project Structure (P0)

1. CRITICAL: Recommended component organization:
   ```
   components/
   ├── ui/                # shadcn/ui base components
   │   ├── button.tsx     # Generated by shadcn/ui CLI
   │   ├── card.tsx       # Generated by shadcn/ui CLI
   │   └── dialog.tsx     # Generated by shadcn/ui CLI
   ├── custom/            # Custom components based on shadcn/ui
   │   ├── primary-button.tsx  # Extends ui/button.tsx
   │   └── user-card.tsx       # Composition of ui/card.tsx
   └── providers/         # Context providers for shadcn/ui
       └── theme-provider.tsx
   ```

2. REQUIRED: Component extension pattern:
   ```typescript
   // components/custom/primary-button.tsx
   import { Button, ButtonProps } from "@/components/ui/button";
   import { cn } from "@/lib/utils";
   
   export interface PrimaryButtonProps extends ButtonProps {}
   
   export function PrimaryButton({ 
     className, 
     children, 
     ...props 
   }: PrimaryButtonProps) {
     return (
       <Button 
         className={cn("bg-primary text-primary-foreground font-bold", className)} 
         {...props}
       >
         {children}
       </Button>
     );
   }
   ```

3. ESSENTIAL: Component composition pattern:
   ```typescript
   // components/custom/user-card.tsx
   import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
   import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
   
   interface UserCardProps {
     name: string;
     email: string;
     avatarUrl?: string;
   }
   
   export function UserCard({ name, email, avatarUrl }: UserCardProps) {
     return (
       <Card>
         <CardHeader className="flex flex-row items-center gap-4">
           <Avatar>
             {avatarUrl ? <AvatarImage src={avatarUrl} alt={name} /> : null}
             <AvatarFallback>{name.slice(0, 2).toUpperCase()}</AvatarFallback>
           </Avatar>
           <div>
             <CardTitle>{name}</CardTitle>
             <p className="text-sm text-muted-foreground">{email}</p>
           </div>
         </CardHeader>
         <CardContent>
           <p>Additional user information here.</p>
         </CardContent>
       </Card>
     );
   }
   ```

## 3. Theming and Styling (P0)

1. CRITICAL: Global CSS core variables:
   ```css
   /* app/globals.css - minimal version */
   @tailwind base;
   @tailwind components;
   @tailwind utilities;
   
   @layer base {
     :root {
       --background: 0 0% 100%;
       --foreground: 240 10% 3.9%;
       --primary: 240 5.9% 10%;
       --primary-foreground: 0 0% 98%;
       --secondary: 240 4.8% 95.9%;
       --secondary-foreground: 240 5.9% 10%;
       --muted: 240 4.8% 95.9%;
       --muted-foreground: 240 3.8% 46.1%;
       --border: 240 5.9% 90%;
       --radius: 0.5rem;
     }
     
     .dark {
       --background: 240 10% 3.9%;
       --foreground: 0 0% 98%;
       --primary: 0 0% 98%;
       --primary-foreground: 240 5.9% 10%;
       --secondary: 240 3.7% 15.9%;
       --secondary-foreground: 0 0% 98%;
       --muted: 240 3.7% 15.9%;
       --muted-foreground: 240 5% 64.9%;
       --border: 240 3.7% 15.9%;
     }
   }
   
   @layer base {
     * {
       @apply border-border;
     }
     body {
       @apply bg-background text-foreground;
     }
   }
   ```

2. REQUIRED: Theme provider implementation:
   ```typescript
   // components/providers/theme-provider.tsx
   "use client";
   
   import { ThemeProvider as NextThemesProvider } from "next-themes";
   import { type ThemeProviderProps } from "next-themes/dist/types";
   
   export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
     return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
   }
   ```

3. ESSENTIAL: Theme switcher component:
   ```typescript
   // components/theme-switch.tsx
   "use client";
   
   import { useTheme } from "next-themes";
   import { Button } from "@/components/ui/button";
   import { MoonIcon, SunIcon } from "lucide-react";
   
   export function ThemeSwitch() {
     const { theme, setTheme } = useTheme();
     
     return (
       <Button
         variant="ghost"
         size="icon"
         onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
       >
         <SunIcon className="h-5 w-5 rotate-0 scale-100 transition-all dark:-rotate-90 dark:scale-0" />
         <MoonIcon className="absolute h-5 w-5 rotate-90 scale-0 transition-all dark:rotate-0 dark:scale-100" />
         <span className="sr-only">Toggle theme</span>
       </Button>
     );
   }
   ```

## 4. Component Customization (P1)

1. CRITICAL: Custom variants pattern:
   ```typescript
   // components/ui/custom-button.tsx
   import { cva } from "class-variance-authority";
   import { Button } from "@/components/ui/button";
   import { cn } from "@/lib/utils";
   
   const customButtonVariants = cva(
     "flex items-center gap-2 font-medium",
     {
       variants: {
         variant: {
           brand: "bg-brand text-white hover:bg-brand/90",
           success: "bg-green-600 text-white hover:bg-green-700",
         },
         size: {
           xs: "h-7 px-2 text-xs",
           xl: "h-14 px-8 text-lg",
         },
       },
     }
   );
   
   export function CustomButton({ className, variant, size, ...props }) {
     return (
       <Button
         className={cn(variant && customButtonVariants({ variant, size }), className)}
         {...props}
       />
     );
   }
   ```

2. REQUIRED: Essential utility function:
   ```typescript
   // lib/utils.ts
   import { type ClassValue, clsx } from "clsx";
   import { twMerge } from "tailwind-merge";
   
   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```

## 5. Integration with Next.js (P1)

1. CRITICAL: Server component pattern:
   ```typescript
   // app/users/page.tsx
   import { db } from "@/lib/db";
   import { UserCard } from "@/components/custom/user-card";
   
   export default async function UsersPage() {
     // Server Component can fetch data directly
     const users = await db.user.findMany();
     
     return (
       <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
         {users.map((user) => (
           <UserCard
             key={user.id}
             name={user.name}
             email={user.email}
             avatarUrl={user.avatar}
           />
         ))}
       </div>
     );
   }
   ```

2. REQUIRED: Client component wrapper:
   ```typescript
   // components/custom/dialog-wrapper.tsx
   "use client";
   
   import { useState } from "react";
   import {
     Dialog,
     DialogContent,
     DialogHeader,
     DialogTitle,
     DialogTrigger,
   } from "@/components/ui/dialog";
   
   interface DialogWrapperProps {
     trigger: React.ReactNode;
     title: string;
     children: React.ReactNode;
   }
   
   export function DialogWrapper({ trigger, title, children }: DialogWrapperProps) {
     const [open, setOpen] = useState(false);
     
     return (
       <Dialog open={open} onOpenChange={setOpen}>
         <DialogTrigger asChild>{trigger}</DialogTrigger>
         <DialogContent>
           <DialogHeader>
             <DialogTitle>{title}</DialogTitle>
           </DialogHeader>
           {children}
         </DialogContent>
       </Dialog>
     );
   }
   ```

## 6. Performance Optimization (P2)

1. REQUIRED: Component code splitting:
   ```typescript
   // app/dashboard/page.tsx
   import { Suspense } from "react";
   import { Skeleton } from "@/components/ui/skeleton";
   import dynamic from "next/dynamic";
   
   // Dynamically import heavy components
   const DataTable = dynamic(() => import("@/components/data-table"), {
     ssr: false,
     loading: () => <Skeleton className="h-[500px] w-full" />,
   });
   
   export default function DashboardPage() {
     return (
       <div>
         <h1>Dashboard</h1>
         <Suspense fallback={<Skeleton className="h-[500px] w-full" />}>
           <DataTable />
         </Suspense>
       </div>
     );
   }
   ```

2. RECOMMENDED: Server-only components:
   ```typescript
   // components/server-only/data-display.tsx
   import "server-only";
   
   import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
   
   interface DataDisplayProps {
     title: string;
     data: unknown;
   }
   
   export function DataDisplay({ title, data }: DataDisplayProps) {
     return (
       <Card>
         <CardHeader>
           <CardTitle>{title}</CardTitle>
         </CardHeader>
         <CardContent>
           <pre className="text-sm">{JSON.stringify(data, null, 2)}</pre>
         </CardContent>
       </Card>
     );
   }
   ```

## 7. Best Practices and Maintenance (P2)

1. REQUIRED: Component versioning:
   ```bash
   # When updating shadcn/ui components:
   cp components/ui/button.tsx components/ui/button.tsx.bak
   pnpx shadcn-ui@latest add button --overwrite --yes
   # Compare and merge custom modifications if needed
   ```

2. ESSENTIAL: Testing pattern:
   ```typescript
   // tests/component/button.test.tsx
   import { render, screen } from "@testing-library/react";
   import userEvent from "@testing-library/user-event";
   import { Button } from "@/components/ui/button";
   
   describe("Button component", () => {
     it("renders correctly with default variant", () => {
       render(<Button>Click me</Button>);
       const button = screen.getByRole("button", { name: /click me/i });
       expect(button).toBeInTheDocument();
       expect(button).toHaveClass("bg-primary");
     });
     
     it("handles click events", async () => {
       const handleClick = jest.fn();
       render(<Button onClick={handleClick}>Click me</Button>);
       
       await userEvent.click(screen.getByRole("button", { name: /click me/i }));
       expect(handleClick).toHaveBeenCalledTimes(1);
     });
   });
   ```
   